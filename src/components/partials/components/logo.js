import React from "react";
import logo from "../../../assets/images/logo.png";

const Logo = (props) => {
  return <img src={logo} className="theme-color-default-img " />;
};

export default Logo;
